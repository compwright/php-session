default:
  suites:
    concurrency:
      paths:
        - 'features/concurrency.feature'
      contexts:
        - 'Compwright\PhpSession\Tests\ConcurrencyContext'
    gc:
      paths:
        - 'features/gc.feature'
      contexts:
        - 'Compwright\PhpSession\Tests\GarbageCollectionContext'
    id:
      paths:
        - 'features/id.feature'
      contexts:
        - 'Compwright\PhpSession\Tests\IdContext'
    persistance:
      paths:
        - 'features/persistence.feature'
      contexts:
        - 'Compwright\PhpSession\Tests\PersistenceContext'
    regeneration:
      paths:
        - 'features/regeneration.feature'
      contexts:
        - 'Compwright\PhpSession\Tests\RegenerationContext'
